package main.java.units.length;

import main.java.units.Unit;

public class Length extends Unit<LengthUnits> {
    public Length(LengthUnits enumUnit, double amount) {
        super(enumUnit, amount);
    }
}
